<div id="data">
  <div class="ui horizontal divider">Commercial</div>
</div>

<table class="ui celled table">
  <thead>
    <tr>
      <th> Housing Type</th>
      <th>Floor Area (square feet)</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>{{#ui-popup tagName="label" content="Retail & restaurant share of nonresidential development."}}Retail <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_ret}}
        </div>
      </td>
    </tr>
    <tr>
      <td>{{#ui-popup tagName="label" content="Office and health care share of nonresidential development."}}Office or Medical <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_ofcmd}}
        </div>
      </td>
    </tr>
    <tr>
      <td>{{#ui-popup tagName="label" content="Education and institutional share of nonresidential development."}}Educational and Institutional <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_edinst}}
        </div>
      </td>
    </tr>
    <tr>
      <td>{{#ui-popup tagName="label" content="Industrial or manufacturing share of nonresidential development."}}Industrial and Manufacturing <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_indmf}}
        </div>
      </td>
    </tr>
    <tr>
      <td>{{#ui-popup tagName="label" content="Warehouse or trucking share of nonresidential development ."}}Warehouse and Shipping <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_whs}}
        </div>
      </td>
    </tr>
    <tr>
      <td>{{#ui-popup tagName="label" content="Research and development (including medical research) share of nonresidential development."}}Research and Development <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_rnd}}
        </div>
      </td>
    </tr>
    <tr>
      <td>{{#ui-popup tagName="label" content="Other uses share of nonresidential development."}}Other <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_other}}
        </div>
      </td>
    </tr>
    <tr>
      <td>{{#ui-popup tagName="label" content="Hotel share of nonresidential development."}}Hotel <i class="help circle icon"></i>{{/ui-popup}}
        {{#if (or (eq model.status 'completed') (eq model.status 'in_construction'))}}
          <span style="color: red;">*</span>
        {{/if}}
      </td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.fa_hotel}}
        </div>
      </td>
    </tr>
  </tbody>
</table>

<table class="ui very basic collapsing celled table">
  <tbody>
    <tr>
      <td>{{#ui-popup tagName="label" content="Number of hotel rooms."}}Hotel Rooms <i class="help circle icon"></i>{{/ui-popup}}</td>
      <td>
        <div class="ui input">
          {{input type="text" value=model.hotelrms}}
        </div>
      </td>
    </tr>
  </tbody>
</table>
