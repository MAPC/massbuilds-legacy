- if flash
  - flash.each do |key, value|
    %div{class: "alert alert-#{key}"}
      %a x
      = value

:javascript

  $(document).on('ready', function () {
    $( document ).on('click', '.removable', function() {

      if (window.cancel_timeout) {
        clearTimeout(window.cancel_timeout);
      }
      $( this ).parent().slideUp();
    });

  });
